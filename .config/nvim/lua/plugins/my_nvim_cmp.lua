return {
	-- "hrsh7th/nvim-cmp",
	-- opts = function(_, opts)
	-- 	local cmp = require("cmp")
	-- 	opts.mapping = vim.tbl_extend("force", opts.mapping, {
	-- 		["<C-k>"] = cmp.mapping.select_prev_item(), -- Navigate to the previous suggestion
	-- 		["<C-j>"] = cmp.mapping.select_next_item(), -- Navigate to the next suggestion
	-- 	})
	-- 	return opts
	-- end,
}
